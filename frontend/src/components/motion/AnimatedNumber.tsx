// Canonical implementation is PopNumber in components/motion.tsx (the
// original transitions.dev "number pop-in"). Re-exported under its
// semantic name for use in dashboard/billing/attendance summary cards.
export { PopNumber as AnimatedNumber } from "@/components/motion";
