// The sliding-pill segmented control already lives at components/motion/TabsMotion
// (transitions.dev "tabs sliding"). Re-exported under this name so the ui/
// Premium* family reads as one consistent system.
export { TabsMotion as PremiumTabs, type SlidingTabOption as PremiumTabOption } from "@/components/motion/TabsMotion";
