// Canonical implementation lives in components/motion.tsx (this project's
// original transitions.dev integration). Re-exported here so every motion
// primitive has one consistent import path: @/components/motion/*.
export { Reveal } from "@/components/motion";
